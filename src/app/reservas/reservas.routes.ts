import { Routes } from "@angular/router";
import { ReservasComponent } from "./reservasComponent/reservas.component";

export const RESERVAS_ROUTES: Routes = [
    { path: '', component: ReservasComponent }
];